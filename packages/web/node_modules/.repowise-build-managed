This tracked marker intentionally keeps RepoWise's integrated `serve` command from installing JavaScript dependencies inside the synced source tree. The Tutor branch's web build stages dependencies and compiles outside OneDrive, then copies only the completed standalone bundle back.
